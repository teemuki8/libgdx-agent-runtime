package io.github.teemuki8.libgdx.agent.runtime.core;

/** Closed semantic action parameter types. */
public enum ActionParameterType {
    BOOLEAN,
    INTEGER,
    DECIMAL,
    STRING,
    ENUM,
    ENTITY_ID
}
